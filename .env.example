# Solana / Anchor (program tests & scripts) — NEVER commit real keys
ANCHOR_PROVIDER_URL=http://127.0.0.1:8899
ANCHOR_WALLET=~/.config/solana/id.json

# Deploy / initialize scripts (placeholders — replace before mainnet)
CLUSTER=devnet
PROGRAM_ID=DPcFT7E8GWzzgUfzP3M1VgBnipr8eR5Y4yv5f28wRt7k
FEE_BPS=600
# Prefer a multisig; do not invent production keys here
FEE_RECIPIENT=REPLACE_WITH_FEE_MULTISIG_PUBKEY
